import HeaderInfoChoice from "./HeaderInfoChoice";
import HeaderInfoText from "./HeaderInfoText";

const HeaderInfo = () => {
  return (
    <section>
      <HeaderInfoText />
      <HeaderInfoChoice />
    </section>
  );
};

export default HeaderInfo;
